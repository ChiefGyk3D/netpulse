# InfluxDB clients (both versions supported)
influxdb-client==1.50.0  # InfluxDB 2.x (token-based auth)
influxdb==5.3.2          # InfluxDB 1.x (username/password auth)
# Pulled in by influxdb; floor it at the first version patched for
# GHSA-6v7p-g79w-8964 (Trivy flagged the image at 1.1.2).
msgpack>=1.2.1

# HTTP requests
requests==2.34.2

# Scheduling (daemon mode only)
schedule==1.2.2
